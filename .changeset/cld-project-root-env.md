---
"chainlink-deployments-framework": minor
---

Add `CLD_PROJECT_ROOT` env var to override domain folder root resolution, and unify the workspace-root resolution used by durable pipeline input generation onto the domain path helpers.
