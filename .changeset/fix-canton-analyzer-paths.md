---
"chainlink-deployments-framework": patch
---

fix(canton): update experimental/analyzer to use chainlink-canton's restructured contracts/v2 bindings path, and migrate engine/cld/legacy CLI commands from olekukonko/tablewriter's v0.0.5 API to v1.x so no replace directive is needed
