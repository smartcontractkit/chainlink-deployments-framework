---
"chainlink-deployments-framework": minor
---

fix(cld)!: remove Archive() method from RegistryProvider interface

Migration guide:
- Remove Archive() method from RegistryProvider interface implementations
